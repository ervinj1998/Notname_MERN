function sum(a, b) {
    return a+b;   //Funcion de suma que devuelve una suma.
}

module.exports ={  //esto es para exportar un modulo y usarlo en otro js. a esto s ele llama regactorizacion tambien.
    sum
};